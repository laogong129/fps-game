# FPS-Game 项目说明

## 项目简介
浏览器第一人称"幸存者围城"游戏：固定竞技场无尽刷怪，击杀升级，三选一强化，10 波通关。

技术栈：Vite + Three.js（纯 JS），单页应用，`npm run dev` 运行。

完整规格见 [PROJECT.md](PROJECT.md)（敌人/武器/场景建模时读取）。

## 玩法规格（顶层摘要）
- 操作：WASD 移动，Shift 冲刺，空格跳跃，鼠标瞄准，左键射击（±14° 辅助瞄准），R 换弹，1/2/3 切枪
- 武器：手枪（基础）/ 霰弹枪 / 步枪（升级解锁，各枪独立成长）
- 敌人：快怪/血牛/喷吐者/分裂体/冲刺者/Boss（第 8 波），几何体造型+颜色区分
- 波次：每 30s 一波，数量递增，上限 40 同时存活，敌人血量不随波次成长
- 升级：经验球吸取，三选一（伤害/射速/弹匣/移速/最大血/解锁枪）
- 掉落：经验球 + 小血包(20%) + 金血包(血牛必掉) + 武器箱(第 3/6/9 波)
- 胜利：存活到第 10 波；死亡显示结算，Enter 重开
- 玩家：100 血，被碰 -15，0.5s 无敌帧，不秒杀

### 明确不做
多人联机 / BGM / 模型贴图资产 / 存档 / 4 种以上枪械 / 角色技能

## 开发规矩
- 【重要决定必须先问用户】方向性改动（新系统/删功能/新依赖）先列选项等拍板；数值微调、bug 修复可直接做
- 【游戏内容设计一律提供方案】新敌人/武器/机制由我提供 2-4 个可选方案，用户拍板后实现
- **【先问清楚再做】** 任何有不确定性的事（参数、位置、触发条件、优先级）必须先确认，复述理解等用户说「对的」再动手
- 【逐步教学】用户是游戏开发小白，每完成一块解释 2-3 个概念，用项目实际代码举例
- 代码改完后直接继续任务，不自动提交
- **只在用户明确说"存档"或"提交"时才执行：**
  1. 运行 `tools/test-engineer.mjs`（语法+构建检查）
  2. 运行 `tools/quality-engineer.mjs`（规范检查）
  3. `git commit` → `git push origin master`
- 单文件不超过 ~300 行，按职责拆模块：`src/game/`（player, enemy, weapon, spawner, levelup, huds）+ `src/main.js` 入口
- 游戏循环 `requestAnimationFrame`，逻辑与渲染分离，`update(dt)` 用秒为单位
- 数值集中在 `src/config.js`，手感问题优先调这里
- 不加注释，除非解释非显而易见的取舍

## 运行方式
- `npm run dev` 启动开发服务器，浏览器打开游玩

## 工具路径
- Blender：`E:\blender\blender-5.2.1-windows-x64\blender.exe`

## 视觉风格（已实现，保持）
- 日式黄昏道场：木板地/朱红木墙/紫雾黄昏/场地中央鸟居/四角白灯笼/樱花树/花瓣粒子飘落
- 配色在 `config.js` `arena` 块，换色即换风格
- 敌人发光双眼（Boss 红眼+双角），枪为多部件几何体
- 手感：相机后坐上跳、命中火花、枪口闪光片、瞄准呼吸晃动、受击屏幕红晕、合成音效