# FPS-Game 项目说明

## 项目简介
浏览器第一人称"幸存者围城"游戏：固定小竞技场中无尽刷怪，击杀得经验，升级三选一，一局约 5 分钟。

技术栈：Vite + Three.js（纯 JS，不用 TypeScript），单页应用，无需构建产物即可 `npm run dev` 游玩。

## 玩法规格（MVP 范围，按此开发，不要超纲）
- 玩家：WASD 移动，Shift 冲刺（×1.6），鼠标瞄准（PointerLockControls），左键射击（±14° 辅助瞄准），R 换弹，1/2/3 切枪
- 武器（3 把，几何体造型，升级"解锁新枪"获得）：
  - 手枪（1）：伤害 30，弹匣 8，射速 1 发/秒，换弹 1.2s
  - 霰弹枪（2）：单发伤害 12×6  pellets，散射 0.12 rad，弹匣 4，换弹 1.6s
  - 步枪（3）：伤害 24，弹匣 30，射速 5 发/秒，换弹 1.8s
- 敌人（全部几何体 + 材质色区分，不用模型资产）：
  - 快怪：红色小方块，血 30，直冲玩家
  - 血牛（波次 3+）：紫色大立方体，血 100，速度慢
  - 喷吐者（波次 4+）：绿色方块，停在 14m 外抛射抛物线飞弹（2.5s 一次，命中 -10）
  - 分裂体（波次 5+）：橙色方块，血 60，死后炸出 2 只小碎片（血 15、高速）
  - 冲刺者（波次 6+）：玫红色方块，近身时蓄力 0.9s（发光预警）后高速冲刺
  - 血牛/喷吐者/分裂体/冲刺者按波次表（config.js `wave.table`）概率混入，未解锁部分全算快怪
- 刷怪：波次制，每 30s 一波，数量与血量随波次递增，上限同时存活 40 只
- 经验：击杀掉落经验球（小发光球体），走近自动吸取
- 升级三选一（随机出 3 个，已解锁的枪不再出现）：伤害 +5 / 射速 +20% / 弹匣 +3 / 移速 +10% / 最大血量 +20 / 解锁霰弹枪 / 解锁步枪（解锁类只作用于当前手持枪升级，切枪后各枪独立）
- 玩家血量 100，被碰 -15（有 0.5s 无敌帧，不秒杀），死亡出结算界面（本局波次 / 总击杀 / 用时），Enter 重开
- HUD：血条、弹药数、当前枪名、经验条、波次号、击杀数（DOM 覆盖层，不用画进 3D）

### 明确不做（即使提到也先拒绝，等 MVP 跑通再议）
- 多人联机 / 网络同步
- 音效与 BGM（第一版）
- 模型 / 贴图资产（全部几何体）
- 存档 / 账号
- 4 种以上枪械、角色技能

## 开发规矩
- 【重要决定必须先问用户】改动玩法数值之外的方向（新增系统、改技术选型、删功能、引入新依赖）前，向用户列出选项和理由，等用户拍板。数值微调、bug 修复可直接做
- 【逐步教学】用户是游戏开发小白，每完成一块就解释 2-3 个游戏开发概念（如"什么是游戏循环"），用项目里的实际代码举例
- 每个功能点完成即 git commit（信息写清楚做了什么，中文即可）
- 单文件不超过 ~300 行，按职责拆模块：`src/game/`（player, enemy, weapon, spawner, levelup, huds）+ `src/main.js` 入口
- 游戏循环用 `requestAnimationFrame`，逻辑与渲染分离，`update(dt)` 用秒为单位
- 数值全部集中在 `src/config.js`，方便调平衡
- 不加注释，除非解释非显而易见的取舍

## 运行方式
- `npm run dev` 启动开发服务器，浏览器打开游玩
- 手感问题（速度/伤害/射速）优先调 `src/config.js`，不要散在各处改
